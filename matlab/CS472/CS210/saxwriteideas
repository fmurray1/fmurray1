while(!</database>)

	if (< && table)
		new table content handler


table content handler 

if(< && table name)
tablename = what ever is between the > and < // all of the following imply this

if(< && field)

String of fields += field content handler data

then </table> reached define table(tablename, string of fields)


field content handler

if (< && field name)
field name = field name

if (< && type)
type = type

if( < && size)

size equals size

return a strimg of fieldname +" " +type;


read moreheads docs on the look for end element it should help

